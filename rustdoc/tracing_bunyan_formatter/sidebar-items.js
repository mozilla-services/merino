window.SIDEBAR_ITEMS = {"enum":[["Type","The type of record we are dealing with: entering a span, exiting a span, an event."]],"struct":[["BunyanFormattingLayer","This layer is exclusively concerned with formatting information using the Bunyan format. It relies on the upstream `JsonStorageLayer` to get access to the fields attached to each span."],["JsonStorage","`JsonStorage` will collect information about a span when it’s created (`new_span` handler) or when new records are attached to it (`on_record` handler) and store it in its `extensions` for future retrieval from other layers interested in formatting or further enrichment."],["JsonStorageLayer","This layer is only concerned with information storage, it does not do any formatting or provide any output."]]};